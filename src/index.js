import React from "react";
import ReactDOM from "react-dom/client";

import {PartTwo} from "./part2/part2";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
    <React.StrictMode>
        <PartTwo/>
    </React.StrictMode>
);
